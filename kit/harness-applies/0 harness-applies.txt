harness login --api-key $HARNESS_API_KEY --account-id $HARNESS_ACCOUNT_ID

harness environment --file environment-dev.yml apply
harness environment --file environment-prod.yml apply

harness connector --file connector-instruqt-k8.yml apply
  had to "Enter valid delegate name" chose the delegateSelectors "helm-delegate" and it seemed to work

harness service --file service-backend.yml --org-id default --project-id instruqt2 apply


infrastructure k8s dev is found via the dev environment
- pasting it in via the ui yaml option under the dev env

templates not supported
harness template --file template-compile-application.yml apply
